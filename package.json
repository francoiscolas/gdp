{
  "devDependencies": {
    "electron": "^1.4.5",
    "electron-builder": "^7.24.1",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.1",
    "gulp-include": "^2.3.1",
    "gulp-sass": "^2.3.2"
  },
  "build": {
    "compression": "maximum",
    "linux": {
      "target": "AppImage"
    },
    "win": {
      "target": "nsis",
      "extraResources": [{"from": "build/win", "to": ".", "filter": ["**/*"]}]
    },
    "nsis": {
      "oneClick": false
    }
  },
  "scripts": {
    "app": "./node_modules/.bin/gulp run",
    "pack": "./node_modules/.bin/gulp pack",
    "dist": "./node_modules/.bin/gulp dist"
  }
}
